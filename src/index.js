import './scss/global.scss'

const htmlClasses = document.getElementsByTagName('HTML')[0].className;
const fred = (htmlClasses == 'fred--active');

if(!fred){
    // Fred Disabled Scripts
}else{
    // Fred Enabled Scripts
    
}